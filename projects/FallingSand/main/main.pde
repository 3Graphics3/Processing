void setup()
{
  surface.setTitle("Falling Sand");
  surface.setResizable(false);
  surface.setLocation(displayWidth / 3, floor(0.1 * displayHeight));

  size(720, 720);
}

void draw()
{
  background(0);
}
